/**
 * The one place the API location is defined. The API listens on 5080 over HTTP and
 * allows this origin via CORS; change both together if you move it.
 */
export const API_BASE = 'http://localhost:5080/api/v1';
